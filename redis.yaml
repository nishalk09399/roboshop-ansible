- name: configuring the redis item
  hosts: redis
  become: yes
  tasks: 

  - name: Add repository
    ansible.builtin.yum_repository:
      name: rpm
      description: RPM repo
      baseurl: https://rpms.remirepo.net/enterprise/remi-release-8.rpm
  